import React from "react";
import styles from "./footer.module.css";
import type { FC } from "react";

const Footer: FC = () => {
  return <footer className={styles.footerRoot}>حقوق برای ما محفوظ است</footer>;
};

export default Footer;
